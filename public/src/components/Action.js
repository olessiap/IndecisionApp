import React from 'react';

const Action = () => {
    return(
        <div>
            <button>What should I do?</button>
            <button>Remove All</button>
        </div>
    )
};

export default Action;